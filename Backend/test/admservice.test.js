// const { describe, expect, it } = require('@jest/globals')
// const AdmService = require("../src/services/AdmService")

// describe('Testes Cliente e Cachorro', () => {

//    const servico = new AdmService()

   // it('Criar cliente', async () => {
   //    const result = await servico.CreateAdm(1)

   //    expect(result.id).toBe(1);
   //    expect(result.nome).toBe('João da Silva');
   //    expect(result.telefone).toBe('987651234');
   // })

   // it('Mostrar os dados dos cachorros', async () => {
   //    const result = await servico.UpdateAdm(1)

   //    expect(result.id).toBe(1);
   //    expect(result.nome).toBe('Pingo');
   //    expect(result.raca).toBe('viralata');
   //    expect(result.cliente).toBe('João da Silva')
   // })

   // it('Mostrar os dados dos cachorros', async () => {
   //    const result = await servico.DeleteAdm(1)

   //    expect(result.id).toBe(1);
   //    expect(result.nome).toBe('Pingo');
   //    expect(result.raca).toBe('viralata');
   //    expect(result.cliente).toBe('João da Silva')
   // })

   // it('Mostrar os dados dos cachorros', async () => {
   //    const result = await servico.GetAdm(1)

   //    expect(result.id).toBe(1);
   //    expect(result.nome).toBe('Pingo');
   //    expect(result.raca).toBe('viralata');
   //    expect(result.cliente).toBe('João da Silva')
   // })

   // it('Mostrar os dados dos cachorros', async () => {
   //    const result = await servico.GetByEmail(1)

   //    expect(result.id).toBe(1);
   //    expect(result.nome).toBe('Pingo');
   //    expect(result.raca).toBe('viralata');
   //    expect(result.cliente).toBe('João da Silva')
   // })

   // it('Mostrar os dados dos cachorros', async () => {
   //    const result = await servico.GetAdmById(1)

   //    expect(result.id).toBe(1);
   //    expect(result.nome).toBe('Pingo');
   //    expect(result.raca).toBe('viralata');
   //    expect(result.cliente).toBe('João da Silva')
   // })

   // it('Mostrar os dados dos cachorros', async () => {
   //    const result = await servico.FindAdmById(1)

   //    expect(result.id).toBe(1);
   //    expect(result.nome).toBe('Pingo');
   //    expect(result.raca).toBe('viralata');
   //    expect(result.cliente).toBe('João da Silva')
   // })

   // it('Mostrar os dados dos cachorros', async () => {
   //    const result = await servico.LogoutAdm(1)

   //    expect(result.id).toBe(1);
   //    expect(result.nome).toBe('Pingo');
   //    expect(result.raca).toBe('viralata');
   //    expect(result.cliente).toBe('João da Silva')
   // })
// })